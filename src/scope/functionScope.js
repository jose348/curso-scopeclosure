function greeting() {
    let userName = 'Alfredo';
    console.log(userName);


    if (userName === 'Aledo') {
        console.log(`Hello ${userName}`);
    } else {
        console.log(`No es ${userName}`);
    }
}
greeting();